<!--pages/dailyhealth/dailyhealth.wxml-->
<van-tabs active="{{ active }}" bind:change="onChange">
  <van-tab title="昨日小结">
    <view class="text">昨日完成事项:{{lastNum}}</view>
    <canvas canvas-id="pieCanvas-num" class="canvas" style="height:300px" bindtouchstart="touchHandler"></canvas>
    <view class="text">昨日工作量:{{lastHour}}</view>
    <canvas canvas-id="pieCanvas" class="canvas" style="height:300px" bindtouchstart="touchHandler"></canvas>
  </van-tab>
  <van-tab title="事件统计">
    <text class="text">工作量趋势</text>
    <view class="workload">
      <canvas canvas-id="lineCanvas" disable-scroll="true" class="canvas" bindtouchstart="touchHandler" bindtouchmove="moveHandler" bindtouchend="touchEndHandler-pie"></canvas>
    </view>
    <!-- TODO：再加一个每日完成事件表line chart，能够筛选时间范围，显示完成事件的类型分布 -->
  </van-tab>
  <van-tab title="报告">
    <van-card
      tag="未查看"
      desc="描述信息"
      title="2023秋第9周(11/27-12/3)健康周报"
      thumb="{{ imageURL }}"
    >
      <view slot="footer">
        <van-button size="mini">详情</van-button>
        <van-button size="mini">删除</van-button>
      </view>
    </van-card> 
  </van-tab>
</van-tabs>